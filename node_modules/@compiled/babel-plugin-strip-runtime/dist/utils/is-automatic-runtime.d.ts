import * as t from '@babel/types';
/**
 * Returns `true` if `node` looks like a `jsx()` like call expression.
 *
 * @param node
 * @param func
 * @returns
 */
export declare const isAutomaticRuntime: (node: t.Node, func: 'jsx' | 'jsxs') => node is t.CallExpression;
