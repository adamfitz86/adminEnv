import * as t from '@babel/types';
import type { Metadata } from '../../../../../types';
import { createResultPair } from '../../../../create-result-pair';
export declare const evaluatePath: (expression: t.Expression, meta: Metadata, pathName: string) => ReturnType<typeof createResultPair>;
